# http4kbox in Kubernetes

This deployment uses the `http4k-cloudnative` module to automatically configure environmental variables
supplied by Kubernetes.

TODO: Add Dockerfile and example K8S YAML files.