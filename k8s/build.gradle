dependencies {
    compile project(":app")
    compile "org.http4k:http4k-server-jetty:$http4k_version"
}

apply plugin: 'application'
mainClassName = "http4kbox.K8sServerKt"
apply plugin: 'com.github.johnrengelman.shadow'

shadowJar {
    baseName = project.name
    classifier = null
    version = null
    mergeServiceFiles()
}
